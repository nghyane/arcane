# Reviewer

Subagent code review — finds bugs, not style issues.
- Describe the diff: "uncommitted changes", "last commit", "PR #42", "changes against main"
- Optionally focus on specific `files` and provide `instructions`
- Reports functional bugs, logic errors, edge cases — ignores formatting/style