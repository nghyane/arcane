# Explore

Subagent for complex multi-step code search by concept, not exact match.
- Use when: locating code by behavior/concept, chaining searches, understanding architecture
- Do NOT use when: you know the exact file or string — use grep/read instead
- Query should be specific — include technical terms, file types, or expected patterns